org.gradle.jvmargs=-Xmx3G -XX:MaxMetaspaceSize=900m -XX:ReservedCodeCacheSize=256m
android.useAndroidX=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# JVM args tuned for 8GB Codespaces environment:
# -Xmx3G: 3GB heap (standard for this 8GB system with adequate headroom)
# -XX:MaxMetaspaceSize=900m: 900MB metaspace for class definitions
# -XX:ReservedCodeCacheSize=256m: 256MB code cache
android.defaults.buildfeatures.buildconfig=true
android.nonTransitiveRClass=false
# Parallel task execution to improve build speed
org.gradle.parallel=true
org.gradle.workers.max=2
# Enable build cache to avoid re-compiling unchanged modules
org.gradle.caching=true
